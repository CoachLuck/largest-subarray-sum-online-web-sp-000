function largestSubarraySum(arr) {
  let re = /(-.)/
  console.log(arr.join(" ").match(re)[0])

}

function search(arr, idx) {
  if (arr[0]) {

  }
}

		let array = [-2, -3, 4, -1, -2, 1, 5, -3]
console.log(largestSubarraySum(array))
